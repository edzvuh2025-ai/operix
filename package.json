{
  "name": "operix",
  "version": "1.0.0",
  "description": "The Operating System for Roblox Groups",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter=@operix/api-server dev & pnpm --filter=operix dev",
    "build": "pnpm --filter=@operix/db build && pnpm --filter=@operix/api-zod build && pnpm --filter=@operix/api-server build && pnpm --filter=operix build",
    "start": "pnpm --filter=@operix/api-server start",
    "db:migrate": "pnpm --filter=@operix/db migrate",
    "db:seed": "pnpm --filter=@operix/db seed"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=8.0.0"
  }
}
